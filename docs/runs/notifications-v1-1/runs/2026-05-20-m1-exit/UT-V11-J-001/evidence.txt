Probes executed
===============

1) codans status (querying the live socket /tmp/codans-501.sock):
   server             0.2.3
   uptime             32887.8s
   connectedClients   0

   Interpretation: a codans process is running. Its reported
   version is 0.2.3, which corresponds to release commit f839bf3
   ("chore(release): bump to 0.2.3"), not the M1 envelope/migration
   work on feature/notification-settings (HEAD 16ba84c per brief).

2) codans doctor:
   codans                0.2.3
   socket            /tmp/codans-501.sock
   socketReachable   true

3) Live ~/.config/codans/notifications.json shape:
   $ jq 'if type == "array" then
          {top_type:"array", count:length}
        else
          {top_type:"object", keys:(keys), version:.version,
           entries_type:(.entries | type),
           entries_count:(.entries | length)}
        end' ~/.config/codans/notifications.json
   {
     "top_type": "array",
     "count": 500
   }

   Interpretation: the file is a bare JSON array, 315277 bytes,
   mtime 2026-05-20 19:24. Consistent with the running release
   v0.2.3 writer. Does NOT speak to the M1 build's writer.

4) Search for the M1 feature build artefact:
   $ find apps/mac/.build apps/mac/build  -name "codans.app" -type d
   (no results)
   $ find ~/Library/Developer/Xcode/DerivedData -name "codans.app" -type d
   (no results)
   $ ls /Applications/codans.app
   ls: /Applications/codans.app: No such file or directory

   Interpretation: no built .app bundle exists for the M1 feature
   branch. The running app is launched from a previous build whose
   .app bundle is no longer on disk (a common state after Xcode
   index-build cleans intermediate products).

What this run cannot tell you
=============================
- Whether the M1 build, on a fresh install with no
  ~/.config/codans/notifications.json on disk, writes the
  envelope shape (`{"version":1,"entries":[...]}`) on first save
  after one inbox mutation.

To re-run for PASS/FAIL
=======================
- Quit the running release app.
- Back up ~/.config/codans/notifications.json to
  notifications.json.user-backup-2026-05-20 .
- Remove ~/.config/codans/notifications.json .
- Build and launch the M1 build:   make mac-build && make mac-run-app .
- Drive one notification-worthy event onto an unfocused pane (e.g.
  `printf '\033]9;hello\007'` via `codans pane send` to an unfocused pane).
- Wait > 1 s past the inbox debounce.
- Inspect with jq:
    jq 'has("version") and has("entries")
        and (.version == 1)
        and (.entries | type == "array")
        and (.entries | length == 1)' \
      ~/.config/codans/notifications.json
  Expected: true.
- Restore the user's backed-up file when finished.
